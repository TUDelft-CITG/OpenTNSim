version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout

      - run:
          name: Build container
          command: docker build -t transport-network-analysis .

      - run:
          name: Start containers
          command: docker-compose up -d

      - run:
          name: Run tests
          command: |
            docker exec -it transport-network-analysis bash -c "python setup.py test"
            echo printing the current working environment
            echo $PWD
            ls
            sudo mv htmlcov /tmp/artifacts

      - run:
          name: Build docs
          command: |
            docker exec -it transport-network-analysis bash -c "python setup.py docs"
            sudo mv docs/_build/html /tmp/artifacts/docs

      - run:
          name: Generate coverage badge
          command: |
            docker exec -it transport-network-analysis bash -c "coverage-badge -o coverage.svg"
            sudo mv coverage.svg /tmp/artifacts/

      - store_artifacts:
          path: /tmp/artifacts

workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/